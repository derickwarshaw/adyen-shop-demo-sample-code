# Adyen Shop demo

## iOS App

- install CocoaPods
- run `pod install`

## Node.sj App

- Install Node.JS
- Install NPM
- run `npm install`
- save `credentials_template.json` as `credentials.json` and edit it
- install `nodemon` and run `nodemon`
